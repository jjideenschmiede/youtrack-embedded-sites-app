/*
 WidgetConfiguration is an interface that defines the shape of the configuration object for the widget.
 It contains a single property, which is a string representing the URL of the widget.
 */
export interface WidgetConfiguration {
	url: string;
}
